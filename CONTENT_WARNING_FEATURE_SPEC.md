# 回饋／蝦幣非阻擋式內容提醒規格

## 1. 功能目的

當主標或副標出現特定促銷關鍵字時，在編輯介面提醒使用者補充必要資訊。此功能只提供法遵／文案提示，不阻擋下載，也不把提示內容寫入廣告圖片。

## 2. 偵測範圍

- 偵測欄位：主標、副標。
- 偵測方式：將主標與副標合併後檢查關鍵字。
- 只要任一欄位命中就顯示提醒。
- 同時命中回饋類與蝦幣類時，兩則提醒都要顯示。

## 3. 觸發條件與正式文案

### 3.1 回饋類

觸發關鍵字：`回饋` 或 `滿額`

顯示文字：

> 提醒您回饋若有門檻/上限，需於日期後方補充警語。如「單筆須滿$X,XXX」、「最高折$XXX」(若無上限則可不寫)。

### 3.2 蝦幣類

觸發關鍵字：`蝦幣`

顯示文字：

> 提醒您若蝦幣需領券，可於標題補充「領券」關鍵字，例如「領券享10%蝦幣」。

### 3.3 提醒按鈕

按鈕文字必須是：

> 我已了解/已補充▶

## 4. 編輯介面呈現

### 4.1 左側工具列

- 日期／警語輸入框使用橘色框線提示。
- 在日期欄下方顯示命中的完整提醒文字。
- 提醒色與超字紅框不同，避免誤認為禁止下載。

### 4.2 各版位畫布

- 日期元素使用橘色外框標示。
- 提醒列固定顯示在畫板外下方，寬度與畫板相同。
- 提醒列使用深色背景、橘色框線與橘色文字。
- 文字依正常語句自然換行，不得留下大面積無意義空白。
- 按鈕緊接在最後一則提醒文字後方，隨文字流動，不得被推到容器最右側。
- 提醒列可超出原版型範圍；父層預覽必須增加高度，不能裁切提醒。
- 不得遮住主標、副標、日期、LOGO、商品圖或 CTA。

### 4.3 最小可視字級

- 螢幕上實際可視字級至少約 `13px`。
- 原始畫布字級至少 `18px`。
- 依 iframe 縮放比例反算畫布字級，上限 `30px`。

## 5. 按鈕行為

- 點擊「我已了解/已補充▶」後，立即隱藏左側提醒、日期橘框及所有版位的提醒列。
- 隱藏只對目前「主標＋副標」內容暫時有效。
- 主標或副標內容一旦改變，重新檢查並恢復符合條件的提醒。
- 重新整理頁面後可再次提醒；不需要永久記住已關閉狀態。
- 點擊按鈕不得更改主標、副標或日期文字。

## 6. 下載規則

- 此提醒為非阻擋式提示，不可加入下載阻擋條件。
- 使用者未按按鈕也可以正常下載。
- 截圖／匯出開始前，暫時移除日期橘框並隱藏提醒列。
- 截圖完成後，恢復編輯預覽中的提醒狀態。
- 匯出的 PNG、分層圖片或 ZIP 圖片中不得出現橘框、提醒文字或按鈕。
- 提醒文字不得混入日期的實際文案資料。

## 7. 跨畫布同步

- 父頁負責偵測關鍵字並產生 `messages`。
- 父頁透過 `postMessage` 將同一組提醒同步到所有 iframe。
- 新建立或重新載入的 iframe 在 ready 後，必須補送目前提醒。
- 任一畫布按下提醒按鈕後，通知父頁同步隱藏所有畫布與左側提醒。
- 畫布回報包含提醒列後的完整寬高，父頁據此調整預覽容器。

## 8. 與超字警示的差異

| 項目 | 橘色內容提醒 | 紅色超字警示 |
|---|---|---|
| 用途 | 提醒補充文案 | 字數超過版型限制 |
| 是否阻擋下載 | 否 | 是 |
| 是否出現在下載圖片 | 否 | 否 |
| 是否可暫時關閉 | 是 | 否 |
| 提示顏色 | 橘色 | 紅色 |

## 9. 驗收案例

1. 主標包含「回饋」：顯示回饋提醒。
2. 副標包含「滿額」：顯示回饋提醒。
3. 主標或副標包含「蝦幣」：顯示蝦幣提醒。
4. 同時包含「回饋」與「蝦幣」：依序顯示兩則提醒及一顆按鈕。
5. 沒有關鍵字：不顯示橘框、提醒列或按鈕。
6. 按下按鈕：所有畫布與左側提醒同步消失。
7. 按下按鈕後修改主標或副標：符合條件的提醒再次出現。
8. 提醒顯示中下載：下載成功，圖片中沒有提醒 UI。
9. 小尺寸與大尺寸版位：提醒字級可讀、寬度等於畫板且不被裁切。

## 10. 目前專案對應位置

- `jbpbn.html`
  - 關鍵字偵測與正式文案。
  - 左側提醒。
  - 跨 iframe 廣播、ready 補送與 dismiss 同步。
- `js/layout-runtime.js`
  - 畫布橘框、提醒列與按鈕。
  - 畫板外下方定位及預覽尺寸回報。
  - 截圖前隱藏、完成後恢復。

