# 掛標顏色自動切換規範

## 1. 目的

掛標提供紅色與白色兩個視覺版本。系統預設使用紅色，但當掛標所在位置的背景為紅色、接近紅色或深色系時，應自動切換為白色版本，以維持掛標的辨識度與可讀性。

本規範適用於：

- 頁面掛標、標籤、角標與浮動標示；
- 靜態圖片、漸層背景與動態背景；
- 桌面版與行動版介面。

## 2. 版本定義

| 版本 | Token | 使用原則 |
| --- | --- | --- |
| 紅色掛標 | `badge-red` | 預設版本，適用於一般淺色或非紅色背景 |
| 白色掛標 | `badge-white` | 適用於紅色、接近紅色或深色背景 |
| 自動模式 | `auto` | 由系統根據局部背景與對比度自動決定 |

紅色與白色版本的文字、圖示、外框與陰影，應一併使用對應的設計 Token，不可只替換背景色而造成文字對比不足。

## 3. 基本規則

1. 掛標預設使用紅色版本 `badge-red`。
2. 只有在顏色模式為 `auto` 時，系統才執行自動判斷。
3. 系統應分析掛標實際覆蓋區域附近的「局部背景色」，不可使用整張頁面的平均色。
4. 當局部背景屬於紅色系或深色系，且白色版本具備足夠對比度時，切換為 `badge-white`。
5. 當背景為淺紅、粉色或接近白色，使用白色會降低辨識度時，維持 `badge-red`。
6. 當背景無法可靠判斷時，回到預設紅色版本。
7. 使用者手動指定紅色或白色時，手動設定優先於所有自動判斷。

## 4. 顏色判定條件

### 4.1 紅色或接近紅色

背景符合以下條件時，視為紅色系：

- 色相與紅色的距離不超過 `25°`；
- 飽和度至少為 `30%`；
- 可包含紅、暗紅、橘紅、粉紅與偏紅紫色。

若使用 HSV/HSL 判斷，紅色色相需處理 `0°` 與 `360°` 的循環，例如 `350°` 仍屬於接近紅色。

### 4.2 深色系

背景的相對亮度 `relative luminance` 小於或等於 `0.35` 時，視為深色系。

深色系不限於黑色，也包含深藍、深紫、深綠、深灰與深紅等背景。

### 4.3 對比度門檻

自動切換前應檢查掛標與背景的對比度：

- 掛標外框或主要圖形與背景：至少 `3:1`；
- 一般大小的掛標文字與其底色：至少 `4.5:1`；
- 大字或粗體文字：至少 `3:1`。

若白色版本未達最低對比度，即使背景屬於紅色系或深色系，也不可切換為白色，應使用可讀性較高的版本。

## 5. 局部背景採樣規則

1. 以掛標中心為基準，擷取掛標外圍約 `1.5` 倍掛標尺寸的背景區域。
2. 排除掛標本身、文字、圖示、外框與陰影，避免污染採樣結果。
3. 單色背景可使用採樣區域的中位數顏色。
4. 漸層或圖片背景應至少採樣 `9` 個位置。
5. 當超過 `60%` 的有效採樣點符合紅色系或深色系，才視為該掛標位於紅色或深色背景。
6. 有效採樣點不足時，視為無法可靠判斷，使用紅色版本。

## 6. 判斷優先順序

優先順序由高至低如下：

1. 使用者手動指定的顏色；
2. 無障礙與最低對比度要求；
3. 紅色系或深色系背景判定；
4. 自動模式的預設值：紅色。

## 7. 建議判斷流程

```text
resolveBadgeVariant(mode, background):
    if mode == "red":
        return "badge-red"

    if mode == "white":
        return "badge-white"

    localBackground = sampleLocalBackground(background)

    if localBackground.isReliable == false:
        return "badge-red"

    isRedFamily = isNearRed(localBackground)
    isDark = relativeLuminance(localBackground) <= 0.35

    redContrast = contrastRatio("badge-red", localBackground)
    whiteContrast = contrastRatio("badge-white", localBackground)

    if (isRedFamily or isDark) and whiteContrast >= 3.0:
        return "badge-white"

    if redContrast >= 3.0 and redContrast >= whiteContrast:
        return "badge-red"

    if whiteContrast >= 3.0:
        return "badge-white"

    return "badge-red"
```

> `contrastRatio()` 應以掛標實際使用的完整視覺版本計算，包含底色、文字、圖示與外框，不應只比較單一背景色值。

## 8. 動態背景與切換體驗

- 背景變化時，掛標應重新判斷局部背景。
- 顏色判定需加入短暫穩定時間，例如連續 `100 ms` 以上維持同一結果才切換。
- 紅色與白色版本切換時，建議使用 `150–250 ms` 的淡入淡出或顏色過渡效果。
- 不得因背景動畫或滑鼠移動造成掛標頻繁閃爍。
- 若背景變化速度過快，應優先維持上一個已確認的版本。

## 9. 驗收案例

| 背景情境 | 預期版本 | 說明 |
| --- | --- | --- |
| 白色、淺灰、淺藍背景 | 紅色 | 使用預設紅色版本 |
| `#C62828` 暗紅背景 | 白色 | 紅色背景，白色辨識度較高 |
| `#FFEBEE` 淺紅背景 | 紅色 | 白色可能與背景融為一體 |
| 深藍、深紫、深灰背景 | 白色 | 深色背景自動切換 |
| 中等亮度的彩色背景 | 依對比度 | 以可讀性較高的版本為準 |
| 漸層或圖片背景 | 依局部採樣 | 以掛標周圍主要背景區域判定 |
| 使用者手動指定白色 | 白色 | 不受自動判斷覆蓋 |
| 無法取得背景色 | 紅色 | 回到預設版本 |

## 10. 驗收標準

- 預設狀態顯示紅色掛標。
- 紅色、接近紅色與深色背景能自動顯示白色掛標。
- 淺紅或接近白色背景不會錯誤顯示低對比度的白色掛標。
- 掛標文字與圖示在所有支援背景上保持清楚可讀。
- 使用者手動設定能覆蓋自動模式。
- 動態背景下不會出現明顯閃爍或頻繁切換。
- 桌面版與行動版的判斷結果一致，除非兩者實際掛標位置的局部背景不同。
